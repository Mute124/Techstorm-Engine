exitKey = 256; // The key that should close the game. See the documentation for key ids.

input = {
    keyboard = (
        moveForward = 87; // The key that should move the camera forwards
        moveForwardTriggerType = 2; // How should the input system check the input

        moveBackward = 83; // The key that should move the camera backwards
        moveBackwardTriggerType = 2; // How should the input system check the input

        moveLeft = 65; // The key that should move the camera left
        moveLeftTriggerType = 2; // How should the input system check the input

        moveRight = 68; // The key that should move the camera right
        moveRightTriggerType = 2; // How should the input system check the input

        tiltLeft = 0; // The key that should tilt the camera left
        tiltLeftTriggerType = 2; // How should the input system check the input

        tiltRight = 0; // The key that should tilt the camera right
        tiltRightTriggerType = 2; // How should the input system check the input
    );
    mouse = (

    );
    controller = (
        // No key triggers for movement as it is usually the thumbsticks. If you wish to use other buttons, you may place them here (It is not implemented yet though). 

        tiltLeft = 0; // The key that should tilt the camera left
        tiltLeftTriggerType = 2; // How should the input system check the input

        tiltRight = 0; // The key that should tilt the camera right
        tiltRightTriggerType = 2; // How should the input system check the input
    )
}

