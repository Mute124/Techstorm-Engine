#pragma once

#include <raylib.h>

class HelloWorldSample  : public Sample {
public:
	HelloWorldSample() : Sample("HelloWorld") {}

	void init() override {
		
	}
	
	void draw() override {
		// draw a single cube
		
	}
};